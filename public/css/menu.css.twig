#language{
  display: -webkit-inline-box;
  display: -moz-inline-box;
}

.separator{
  min-width: 15px;
  text-align: center;
}

.nav-link:hover.separator:hover{
  color: rgba(255,255,255,.5);
}

#titre-page{
  display: inline-block;
  padding-top: .3125rem;
  padding-bottom: .3125rem;
  margin-right: 1rem;
  font-size: 1.25rem;
  line-height: inherit;
  white-space: nowrap;
  margin: 0;
}

#menu{
  background-color: rgba(35,35,35,0.45);
}